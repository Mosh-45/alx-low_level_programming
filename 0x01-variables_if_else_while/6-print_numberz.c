#include<stdio.h>
/**
 * main - prints all single digit numbers of base 10 starting from 0,
 *  followed by a new line
 *  Return: always 0
 */
int main(void)
{

int h;
for (h = 48; h <= 57; h++)
{
	putchar(h);
}
putchar(10);
return (0);
}
